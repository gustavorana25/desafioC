import styled, { keyframes } from 'styled-components';

export const animations = {
    FadeInCenter: keyframes`
        100% {
            transform: translate(0,0);
            opacity:1; 
        }
    `
}