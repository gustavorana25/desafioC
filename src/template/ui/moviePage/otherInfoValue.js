import styled from 'styled-components';
import { colors, fonts } from '../../styles';

export const OtherInfoValue = styled.p`
    font-family: ${fonts.primary};
    font-size:15px;
    color: ${colors.secondary};
`;